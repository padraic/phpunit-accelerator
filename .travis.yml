dist: trusty

language: php

sudo: false

php:
    - '7.0'
    - '7.1'
    - 'nightly'

matrix:
  fast_finish: true
  allow_failures:
    - php: nightly

before_script:
    - composer update --no-interaction --no-suggest --prefer-dist

script:
    - vendor/bin/phpcs
    - vendor/bin/phpunit
